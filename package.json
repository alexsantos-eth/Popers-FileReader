{
  "name": "Popers",
  "version": "1.0.0",
  "description": "The best custom file explorer for MacOs, Windows, Android and iPhone.",
  "main": "index.js",
  "scripts": {
    "buildMac": "electron-packager . --overwrite --platform=darwin --arch=x64 --prune=true --out=release-builds",
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "AlexSantos",
  "license": "MIT",
  "dependencies": {
    "bootstrap": "^4.1.1",
    "get-image-colors": "^1.8.1",
    "jquery": "^3.3.1",
    "rimraf": "^2.6.2"
  },
  "devDependencies": {
    "electron": "^2.0.3",
    "electron-packager": "^12.1.0"
  }
}
